server:
  port: 8089
spring:
  application:
    name: configdemo
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
  profiles:
    active: prod # 表示向配置中心索要的环境配置
  # 索要配置的格式：${application.name}-${spring.profiles.active}.${file-extension}
